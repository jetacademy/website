# Suggested Commands
This project uses Laravel with Vue.js/Vite.

## Development Server
- Run Laravel: `php artisan serve`
- Run Frontend Dev Server: `npm run dev` (within `web_app` directory)

## Database
- Run Migrations: `php artisan migrate`
- Seed Database: `php artisan db:seed`
